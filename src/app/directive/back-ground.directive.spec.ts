import { AuthGrowDirective } from './auth-grow.directive';

describe('AuthGrowDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthGrowDirective();
    expect(directive).toBeTruthy();
  });
});
